<%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
<table class='table table-striped table-condensed'>
  <thead>
    <tr>
      <th>Barcode</th>
      <th>Supplier Stock Number</th>
      <th>Item Description</th>
    </tr>
  </thead>

  <tbody>
    <% @barcodes.each do |barcode| %>
      <tr>
        <td><%= barcode.id %></td>
        <td><%= barcode.itemfile.Supplier_Stock_Number %></td>
        <td><%= barcode.itemfile.Item_Description_1 %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<hr/>

<%= react_component('Upload', tokenName: request_forgery_protection_token, token: form_authenticity_token)%>
